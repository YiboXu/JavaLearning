server:
  port: 8601

spring:
  cloud:
    alicloud:
      access-key: xxxxx
      secret-key: yyyyy
      oss:
        endpoint: oss-cn-shanghai.aliyuncs.com
        bucket: billhuscw20200428
        path: gulimall-project

management:
  endpoints:
    web:
      exposure:
        include: '*'

info:
  app.name: gulimall
  company.name: www.springlearning.com
  build.artifactId: ${project.artifactId}
  build.version: ${project.version}
